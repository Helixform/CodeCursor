[package]
name = "cursor-core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
node-bridge = { path = "../node-bridge" }
wasm-bindgen = "0.2"
wasm-bindgen-futures = "0.4"
